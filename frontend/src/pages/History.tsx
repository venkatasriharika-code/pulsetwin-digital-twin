import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { api } from "../api/client";
import type { DecisionRecord } from "../api/types";
export default function History() { const [items,setItems]=useState<DecisionRecord[]>([]); useEffect(()=>{api.getDecisions().then(setItems).catch(()=>setItems([]));},[]); return <div><header className="page-header"><div className="eyebrow">OPERATIONS LOG</div><h1>Decision history</h1><p>Scenarios saved by the operations team.</p></header>{items.length===0?<div className="empty-state"><h2>No decisions saved</h2><p>Run a scenario in Twin Optimizer and save the recommendation here.</p><Link className="btn-primary" to="/ask">Run a scenario →</Link></div>:<div className="history-list">{items.map(item=><div className="card history-item" key={item.id}><div><div className="eyebrow">{new Date(item.createdAt).toLocaleString()}</div><h3>{item.recommendation}</h3><p>“{item.question}”</p></div><div className="history-impact"><span>Avg wait <strong>−{item.impact.avgWaitReduction ?? 0}m</strong></span><span>Critical <strong>−{item.impact.criticalWaitReduction ?? 0}m</strong></span></div></div>)}</div>}</div>; }
